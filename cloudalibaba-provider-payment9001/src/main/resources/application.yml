server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #server-addr: localhost:8848 #配置Nacos地址
        server-addr: 139.159.187.229:1111 #配置Nacos注册中心地址，实际为nginx代理地址

management:
  endpoints:
    web:
      exposure:
        include: '*'
